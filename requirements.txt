# ===================================================
# ОСНОВНЫЕ ЗАВИСИМОСТИ ДЛЯ БОТА ПИНКИ ПАЙ
# Автор: MADAO81
# ===================================================

# ----- БИБЛИОТЕКА ДЛЯ МЕССЕНДЖЕРА MAX -----
# Основной фреймворк для работы с MAX API
umaxbot>=0.8.0

# ----- ИИ-ПРОВАЙДЕРЫ (РОССИЙСКИЕ) -----
# GigaChat от Сбера
gigachat>=0.1.0

# YandexGPT (используем aiohttp для прямых запросов)
aiohttp>=3.9.0

# ----- ПАРСИНГ РЕЦЕПТОВ -----
# Для парсинга HTML с andychef.ru
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Для HTTP-запросов (используется в recipe_service.py)
requests>=2.31.0

# ----- РАБОТА С ПЕРЕМЕННЫМИ ОКРУЖЕНИЯ -----
# Загрузка .env файла
python-dotenv>=1.0.0

# ----- ВСПОМОГАТЕЛЬНЫЕ БИБЛИОТЕКИ -----
# Для работы с датами (используется в stats_manager.py)
python-dateutil>=2.8.2

# Для логирования (встроенный модуль, но иногда нужен для продакшена)
# logging уже встроен в Python, ничего не ставим

# Для работы с типами данных (опционально, для аннотаций)
typing-extensions>=4.8.0
